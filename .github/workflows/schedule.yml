name: Schedule_workflow
on:
    schedule:
        - cron: "15 1 * * *"
jobs:
    Test:
        runs-on: ubuntu-latest
        steps:
            - name: test
              run: echo "the schedule test ran"
